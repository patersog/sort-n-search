//import sort from "./sort";
const search = require("./search");
const { array: arrayData } = require("./data/array-data");

const algorithms = {
  search
};

const test = {
  arrayData
};

module.exports = {
  algorithms,
  test
};
